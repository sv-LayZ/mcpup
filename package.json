{
  "name": "mcpup",
  "private": true,
  "version": "0.0.0",
  "description": "Semantic health monitoring for MCP servers — the probe engine and the mcp-check CLI.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "bun test",
    "typecheck": "bun run --filter '*' typecheck",
    "build": "bun run --filter '@mcpup/core' build && bun run --filter '@mcpup/cli' build",
    "check": "bun run packages/cli/src/bin.ts"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "devDependencies": {
    "@types/bun": "1.3.14"
  }
}
